- [x] Retire the embedded-replica machinery (openReplica/bootReplica/freshness gate in db.ts, LAZYBOY_NO_REPLICA) and reassess read-costs ledger + /api/cron/usage-check now that the store is self-hosted — home/day/search all serve from the materialized payload + client memory as of 2026-08-11; verify nothing else still routes through getReader first | self-startable
  - done 2026-08-11: db.ts is one client (getReader/syncReader/openReplica/bootReplica/removeReplicaFiles/chooseReader/backgroundSync/readerBox/REPLICA_* all deleted, ~140 lines); write_seq kept as the cache/payload version and re-documented; ledger + /api/cron/usage-check kept and both marked dormant-against-Turso; CLAUDE/ARCHITECTURE/GOTCHAS/READ_QUOTA updated, BRIDGE addendum added.
- [x] Reword the `client_metrics` comment block in src/lib/db.ts — it still cites `getReader()` and the replica freshness gate. Left untouched on 2026-08-11 because another agent held that region live | self-startable
  - done 2026-08-11 (later that session): header comment now explains the LIVE cache-key/ETag rationale.
- [x] Build the phone search MODE per docs/PHONE_SEARCH_MODE_PLAN.md (Rem-approved 2026-08-13): collapse home bands while searching, results box sized to the visual viewport (--lb-vvh), scroll-to-dismiss keyboard, Cancel affordance; verify with Playwright + the iOS Simulator harness (memory: lazyboy-ios-simulator-testing) on ≥2 device sizes | self-startable
  - done 2026-08-13: shipped with Rem's amendment (day strip stays as the scope filter, greeting/dock/header fold); verified on iPhone 17 Pro/iOS 26.5 (recorded) + 16 Pro/18.6; sim caught + fixed the pill un-docking after scroll-dismiss
